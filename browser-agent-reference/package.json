{
  "name": "browser-agent",
  "version": "1.0.0",
  "description": "Browser-native AI agent with an HTTP tool. Single self-contained HTML file, no backend.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "pretest:e2e": "npm run build",
    "test:e2e": "playwright test",
    "pretest:e2e:real": "npm run build",
    "test:e2e:real": "REAL_MODEL=1 playwright test tests/e2e/real-model.spec.js",
    "serve:dist": "node scripts/serve-dist.js",
    "preeval": "npm run build",
    "eval": "node scripts/tool-eval.js",
    "docs:prompts": "node scripts/sync-prompt-docs.js",
    "check": "npm run test:coverage && npm run test:e2e"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.84"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitest/coverage-v8": "^4.1.10",
    "vite": "^8.2.1",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.10"
  }
}
